# Right half as a BLE split peripheral of the dongle (stock role, explicit).
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
CONFIG_ZMK_SPLIT_BLE=y

# Forward the right trackpad over the split.
CONFIG_ZMK_POINTING=y

CONFIG_ZMK_USB=n
